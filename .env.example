# Bilibili API Configuration
# Copy this file to .env and update with actual API endpoints

# Base URL for the Bilibili extraction API
# 
# IMPORTANT: Replace with actual Bilibili extraction API endpoint
# 
# This application requires a Bilibili video extraction API that provides:
#   POST /extract - Extract video download URLs from Bilibili video links
#   GET /search - Search for Bilibili videos by keyword
# 
# Expected endpoints:
#   POST {VITE_API_BASE_URL}/extract
#     Request: { "link": "https://www.bilibili.com/video/BV..." }
#     Response: { "text": "...", "medias": [...], "overseas": 0 }
# 
#   GET {VITE_API_BASE_URL}/search?q=query&pn=1
#     Response: { "result": { "video": [...] }, "page": {...} }
# 
VITE_API_BASE_URL=https://api.example.com

# API timeout in milliseconds (default: 30000 = 30 seconds)
VITE_API_TIMEOUT=30000

# Enable retry logic for failed requests (default: true)
VITE_API_ENABLE_RETRY=true
